spring:
  application:
    name: api-gateway-from-scratch

management:
  endpoints:
    web:
      exposure:
        include: health,metrics
  endpoint:
    health:
      show-details: never

gateway:
  routing:
    routes: []
  upstream:
    http:
      connect-timeout: 5s
      read-timeout: 30s
